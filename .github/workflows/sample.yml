name: Sample

on:
  pull_request:

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3
    
    - name: exec docker container bash
      run: make bash
    
    - name: exec date
      run: date
